# Origin of the local Express backend (server/src/server.js), used only by
# vite.config.js to configure the dev proxy for /api and /uploads.
# Match this to PORT in server/.env if you've changed it from the default.
VITE_BACKEND_ORIGIN=http://localhost:4000
